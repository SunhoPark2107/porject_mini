CREATE TABLE project_longterm(
naiton VARCHAR(20),
date VARCHAR(20),
total_visitor INTEGER,
age_under20 INTEGER,
ageband_20 INTEGER,
ageband_30 INTEGER,
ageband_40 INTEGER,
ageband_50 INTEGER,
age_over60 INTEGER
);


load data infile 'c:/mysql/project_csv_full.csv'
into table project_longterm
fields terminated by ','
enclosed by '"'
lines terminated by '\n'
ignore 1 rows;

CREATE TABLE project_age_longterm(
date VARCHAR(10),
under20 INTEGER,
ageband_20 INTEGER,
ageband_30 INTEGER,
ageband_40 INTEGER,
ageband_50 INTEGER,
over60 INTEGER
);

load data infile 'c:/mysql/project_csv_full.csv'
into table project_age_longterm
fields terminated by ','
enclosed by '"'
lines terminated by '\n'
ignore 1 rows;
